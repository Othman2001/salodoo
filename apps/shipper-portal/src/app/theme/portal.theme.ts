import { createTheme } from "@nextui-org/react";

const theme = createTheme({
  type: "dark",
  theme: {
    colors: {
      background: "#141414",
      primary: "#FF6813",
    },
  },
});

export default theme;
