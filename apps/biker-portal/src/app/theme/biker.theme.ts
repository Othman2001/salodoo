import { createTheme } from '@nextui-org/react';

const theme = createTheme({
  type: 'dark',
  theme: {
    colors: {
      background: '#141414',
      primary: '#9C7E09',
    },
  },
});

export default theme;
